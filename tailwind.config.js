/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,ts,jsx,tsx}", // Caminho para seus arquivos
  ],
  theme: {
    extend: {}, // Extensões personalizadas (cores, fonts, etc.)
  },
  plugins: [], // Plugins adicionais (opcional)
}